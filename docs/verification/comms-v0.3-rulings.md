# Comms v0.3 rulings register

Every decision taken while executing plan `docs/superpowers/plans/2026-09-24-comms-v0.3.md` (rev 2) that deviates from it, or resolves a contradiction between plan and spec (spec A39). This register records the decision independently of any conversation.

**Pinned at pre-flight:**

| Document | SHA-256 |
|---|---|
| `docs/comms-spec-v0.3.md` (rev 2) | `7a415167bce71862…` |
| `docs/superpowers/specs/2026-09-24-comms-v0.3-design.md` (rev 2) | `66f6a6f5d633a0c0…` |
| `docs/comms-spec-v0.3.md` (rev 2 + R-001, `406d9dc`) | `0a893d8d6a6b92a8…` |
| `docs/superpowers/specs/2026-09-24-comms-v0.3-design.md` (rev 2 + R-001, `406d9dc`) | `b91671682376f102…` |

The last row naming a document is its current pin; `test_v03_preflight.py` fails on any edit that is not pinned here with the ruling that made it.

| ID | Date | Section | Discovery | Decision | Reason | Tests | SHA |
|---|---|---|---|---|---|---|---|
| R-000 | 2026-09-24 | Plan tooling | `executing-plans` helper scripts accept numeric task IDs only; this plan uses A1–D39. | Record each task's BASE and completion in the ledger by hand, after the same test run. | Keeps the execution record without renaming 130 tasks. | — | (this commit) |
| R-001 | 2026-09-24 | Spec A31 / P §4 / design D.4 | Task A1b's disjointness test measured that `msg_` and `tpl_` collide with WhatsVault's `msg` and `tpl` ID prefixes, which reach the context engine through the webhook archive. | Comms uses `cmg_` (message) and `ctp_` (template). The spec (A31 execution amendment), design and plan are amended explicitly. | A single prefix registry must be disjoint (5b-4 §1); an ambiguous ref would misroute reply, edit or delete. | `test_v03_prefixes_disjoint_from_telegram_whatsvault_and_5b4` | (A1b commit) |
| R-002 | 2026-09-24 | Plan A4 / design §A.5 | Reading the legacy chain: profiles need event columns, a checkpoint domain, signed fields and minters, not only two domains. The legacy vocabulary is Telegram-specific. Legacy `evt_` collides with WhatsVault `evt`. | Core holds `ChainProfile`, the engine and `COMMS`. `LEGACY_TELEGRAM` is defined in the Telegram package. Comms audit events use a new prefix `aev_`, and checkpoints `ack_`. The comms checkpoint domain is `comms-audit-checkpoint/v1`. | Transport specifics stay out of core; one engine; a disjoint prefix registry. | `tests/core/audit/test_chain_engine.py` | (A4 commit) |
| R-003 | 2026-09-24 | Plan A10 / design §A.7 | The plan's `LegacyEnv` handed core the Telegram keys and schema; the layering rule forbids that. | Core defines a `LegacyPort` protocol and orchestrates; `TelegramLegacyPort` implements drain, verify, seal and anchor. The legacy seal is a registered setting `audit.append_state` plus a legacy `Migration(3)` whose triggers refuse audit inserts once sealed and make the seal one-way. The legacy `SCHEMA_VERSION` becomes 3. | Core stays transport-neutral; the database, not only application code, enforces the seal (G3). | `tests/core/audit/test_cutover_legacy.py` | (A10 commit) |
| R-004 | 2026-09-24 | Plan A11 / spec A33 | `advance_comms` in the plan took a `LegacySeal` carrying the cut ref; the cut ref lives in `cutover_state` and the seal comes from the port. The plan did not say how `tgml1` retirement survives a crash or an operator re-enable. | `advance_comms(conn, legacy, writer, *, now, crash_at)`. Retirement is database-enforced like the seal: legacy setting `auth.tgml1_state` (`open`/`revoked`) and triggers in the unreleased legacy Migration 3 refuse enabling or adding a bearer client once revoked, one-way. The epoch bumps exactly once; `revoked_count` is the number of bearer registrations retired, so a resumed run records the same payload. Genesis refuses a non-empty comms chain. | Fail closed; replay-safe; the first comms event is always the cutover. | `tests/core/audit/test_cutover_comms.py`, `test_cutover_leases.py`, `test_cutover_crashes.py` | (A11 commit) |
| R-005 | 2026-09-24 | Plan A12, B2 / G6 | A12's truncated-legacy test needs `verify_chain` to start at a signed root, which the plan schedules for B2. Legacy checkpoints have no `reason` column (frozen schema). | `verify_chain(root=)` lands in A12; B2 adds epochs and the five attacks. "Reason `V0_3_CUTOVER`" is realised as: the latest checkpoint signs the chain head, that event is the `system.cutover_final` marker, and `audit.append_state` is `sealed`. Core stays schema-free: the transport supplies `LegacyVerify`. | The spec wins over task order; no legacy schema change. | `tests/core/audit/test_verify_all.py` | (A12 commit) |
| R-006 | 2026-09-24 | Plan A14 / design A.3 | No `comms.cli` module exists. The daemon composed the MCP ingress with the admin socket. | Production roots are the console-script target and the daemon; the static closure counts lazy imports. `build_runtime` moves to `runtime/legacy_composition.py` for retained tests and the smoke. The daemon serves admin only. `demo` retires with `serve`; both exit 8 (`EXIT_RETIRED`). `legacy_verify` uses the core engine directly. | Retired, not deleted (A1); historical verification stands alone (A3). | `tests/security/test_v03_retired_surfaces.py` | (A14 commit) |
| R-007 | 2026-09-24 | Plan A15 / spec A2 | The plan lists `consent*` among commands that answer `RETIRED_IN_V0_3`, but 5b-3 tombstoned them (`UNKNOWN_COMMAND`). `policy export/import` were never built (their bundle wire never shipped and is tombstoned); `exposure status` measures the retired budget. | Retired: all 28 `project`/`scope`/`policy`/`exposure` commands. `consent*` stay tombstoned. `AdminRouter(surface=)` is an injected seam only `legacy_composition` may pass (AST-pinned). | A tombstone is never reassigned a new meaning. | `tests/security/test_v03_retired_authority.py` | (A15 commit) |
| R-A16 | 2026-09-24 | Plan A16 / D4, A1 | First edit inside the WhatsVault subtree since 5b-2. | Removed `apps/mcp`, its launchd unit, the `whatsvault-mcp` script, CODEOWNERS entry and CI assertion; `.env.example` loses `WHATSVAULT_PUBLIC_URL` (read only by the app). 89 WhatsVault test IDs removed with the app, listed in `comms-v0.3-whatsvault-removed-a16.txt` for A21. `src/whatsvault/mcp` (the library) stays. Docs carry a retirement note. | Sanctioned by D4 (retire both old servers) and A1. | `test_whatsvault_mcp_app_is_absent`; WhatsVault suite 450 passed | (A16 commit) |
| R-C27 | 2026-09-25 | Plan C27 / design §C.8 | Second edit inside the WhatsVault subtree. The Meta contract oracle belongs with WhatsVault's own fake. | `src/whatsvault/providers/fake_meta.py` gains `FakeGraph` appended after the unchanged `FakeMeta` (byte-for-byte prefix): every Graph endpoint the comms adapter calls plus the media CDN host, deterministic ids, and HMAC-signed status webhooks; no HTTP library is added to the subtree. `tests/conformance/meta_oracle.py` adapts it to httpx. | Sanctioned by the plan (R-C27); one oracle, owned where the fake already lives. | `tests/conformance/test_meta_oracle.py`; provenance `RULED_MODIFIED["R-C27"]`; WhatsVault suite 450 passed | (C27 commit) |
| R-008 | 2026-09-24 | Plan A17 / spec A1, A2 | "None of the 16 names occurs under `src/` outside `legacy_verify`" contradicts A1/A14: the retained legacy modules and the legacy chain's own event vocabulary (needed to verify it) name them. There is no `issue_lease`; the only `tgml1` issuer was the `auth headers` admin command. | The tool-name rule applies to the production closure (CLI + daemon, lazy imports included) minus the verification closure; the six WhatsVault names must have no registry (`build_tool_handlers`) anywhere. The strict JSON decoder moves to `comms.core.strict_json` (contract re-exports it) so production stops importing the tool contracts. `auth headers` joins `RETIRED_ADMIN_COMMANDS` (29); no production module calls `mint_lease`; the codec stays for verification. The protocol multiset records empty `TOMBSTONED_IN_V0_3`/`ADDED_IN_V0_3` with the reason. | Retired, not deleted (A1); a tombstone binds what production can speak. | `tests/security/test_tombstones.py`, `test_comms_protocol_frozen.py` | (A17 commit) |
| R-A20 | 2026-09-24 | Plan A20 / D5 | The v0.2 no-send assertions contradict D5 (owner_full_admin). The plan also empties `.claude/settings.json`'s always-ask rules for campaign sends. | The AI-boundary suite is rewritten for v0.3: the surface is exactly the catalog, writes and destructive operations (P §23–29 list) are annotated, no raw-RPC tool, MCP handlers reach the service layer only (A37), WhatsVault has no network client. Gateway §31's "AI cannot obtain send primitive" now maps to the service-layer guard. **The `.claude/settings.json` change is deferred for the owner's explicit confirmation**: it loosens the permission guardrail on the executing agent's own actions, so the agent does not make it unasked. No test asserts either state. | D5 governs the product; a change to the agent's own permissions is the owner's to make. | `tests/security/test_ai_boundary.py` | (A20 commit) |
| R-B11 | 2026-09-24 | Plan B11 / spec A13 rev 2, N7 | Measured: `security add-generic-password … -w -` stores the literal `-`; `-w` last makes the keychain argument a usage error, so the CLI cannot take a secret on stdin; the System Keychain needs root; PyObjC is not installed. | No Keychain backend in v0.3: provider credentials live in `FileSecretStore` (daemon-owned 0600 versioned files), sharing one write-once file mechanism with the key slots. The only `security` caller left is the legacy Telegram `api_hash` reader (`keys/keychain.py`), pinned by name until Part C replaces it. | The secret never reaches argv; one versioned-file rule. | `tests/core/keys/test_secret_store.py` | (B11 commit) |
| R-D38 | 2026-09-25 | Plan D38 / design D.4, D.7 | The catalog (P §22–35) has no name-resolution tool, though design D.4 says "resolution tools return `AMBIGUOUS_TARGET`"; the plan's D38 names "the Part D list" without one; D.7's `template` CLI group was never built; the smoke's comms phase never ran `verify --all`. | Ambiguity is refused by the service resolver (`services/resolve.py`) and structurally: every write target is a ref pattern, so a name never reaches a write service (P §81 test). The exit test keys on design D.1–D.11, read from the design. `comms template list\|get\|create\|edit\|delete` maps the `comms_whatsapp_template_*` tools. The smoke runs the cutover at world creation and `verify_all` after every surface wrote. | Adding a resolution tool would change the pinned catalog beyond P; the structural refusal is stronger than a tool that can be skipped. | `tests/evaluation/test_ambiguity_deterministic.py`, `tests/security/test_v03_part_d_exit.py`, `tests/cli/test_cli_operations.py`, smoke `phase_v03_comms` | (D38 commit) |
| R-A20 (decided) | 2026-09-25 | Plan A20 / D5 | The owner ruled on the deferred `.claude/settings.json` change. | Host permission UX is defence in depth. No blanket auto-approval for `comms_*`; read-only tools may be auto-allowed; every consequential write (`requires_request_id` and destructive or open-world: 43 tools) is host-confirmed, generated from the catalog; the CLI campaign-send ask rules are kept. The host prompt is not part of the Comms authorization protocol, and its absence does not weaken Comms's own authentication. | The owner's decision. | `tests/security/test_host_permissions.py` | (E0 commit) |
| R-E1 | 2026-09-25 | Spec A33 / D39-PRE | Part D's evidence listed "a `cml1` lease replayed within 60 s is not tracked" as a gap. | By design: a lease reused within its validity authenticates the same client again. Mutation replay safety comes from `(authenticated_client, request_id)`, never from single-use bearer semantics. No nonce store is added. | The owner's decision: authentication (who) and write idempotency (whether done) are separate. | `tests/core/auth/test_cml1.py`, `tests/services/test_mutations.py` | (E0 commit) |
| R-E2 | 2026-09-25 | C23 follow-up / D39-PRE | Per-recipient template language was carried as a follow-up. | Language is selected explicitly per campaign and template binding; automatic per-recipient language preference is deferred and not claimed in v0.3. | The owner's decision: smaller surface, no inference. | `tests/core/campaigns/test_templates.py` | (E0 commit) |
| R-E3 | 2026-09-25 | Design D.7 / D39-PRE | Part D registered operator commands that answered "not wired in this release". | A registered command has a working handler; a command without one is not registered. Any runbook command resolving to neither is a test failure (E9). | The owner's decision: no decorative CLI. | E9 `tests/cli/test_operator_completeness.py` | (E0 commit) |
| R-E4 | 2026-09-25 | D39-PRE E1 / spec A9–A11 | `keys provision` must run before the daemon exists, yet was routed to the admin socket; and the audited rotation appends to the comms chain, which the cutover requires empty (`cutover.py`: "comms chain is not empty"). | `comms keys provision` is local, under the daemon's runtime lock (it refuses while a daemon runs), idempotent, never overwriting (damaged existing material, or a db key without its database, is refused and left untouched), with 0700 directories and `O_EXCL` files. Before the genesis, first versions are registered without an audit event (one generalized `bootstrap_keys`, which the audit-key bootstrap now calls); after it, a missing purpose is minted by the audited rotation. Purposes: audit-chain, audit-checkpoint, campaign-commit, backup, cursor, oauth-signing, plus comms-db-key. | Approved by the owner (R-E4); the genesis covers pre-chain keys exactly as it always covered the audit keys. | `tests/runtime/test_provision.py` | (E1 commit) |
| R-E5 | 2026-09-25 | D39-PRE E3 | The daemon had no configuration for its ports, the Meta ids or the remote listener. | `comms/comms.json`: non-secret settings only, 0600 in a 0700 directory, owned by the daemon user, not a symlink; strict JSON (duplicate keys refused); a closed typed schema (unknown keys refused); any key whose name reads like a secret (token, secret, key, password, seed) is refused; every listener binds 127.0.0.1 (tunnels forward to it); remote URLs are https. Absent file: loopback defaults, no remote and no webhook listener. No programmatic writer in v0.3. | Approved by the owner (R-E5); kept out of comms.db so an unopenable database never hides how to start. | `tests/runtime/test_settings.py` | (E3 commit) |
