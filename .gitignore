# Appendix B baseline (spec §Appendix B) — Telegram credentials/session state
*.session
*.session-journal
*.session-wal
*.session-shm

# Local configuration/secrets
.env
.env.*
!.env.example
secrets/

# Runtime metadata (repo-root state dir only; src/comms/transports/telegram/runtime/ is source)
/runtime/
*.log

# Python
.venv/
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Phase-1 additions (plan Task 1 Step 4)
dist/
.superpowers/sdd/*/scratch/
docs/verification/scratch/

# Phase-2b consent agent (plan Task 1 Step 5): loose swiftc build scratch
build/

# Subagent-driven execution worktrees (isolated tracks, never committed)
.worktrees/
